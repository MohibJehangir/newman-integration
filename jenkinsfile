#!groovy

pipeline {
    agent any
    tools { 
        maven 'Maven 3.3.9' 
        jdk 'jdk8' 
    }
    
    stages {
        
stage('Newman') {
            steps {
				sh "cd /home/mohib/postman_jenkins npm install -g newman && newman run Get_Req.postman_collection.json"
            }
        }
				
        }
    }
